name: ci

on:
  pull_request:
  push:
    branches: [main]

jobs:
  test:
    name: ${{ matrix.os }} / py${{ matrix.python }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-22.04, macos-13]
        python: ["3.10", "3.11", "3.12", "3.13"]
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python }}

      - name: install meson + ninja
        run: pip install meson ninja

      - name: build wirelog from source
        shell: bash
        env:
          # Pinned to `main` until wirelog cuts the next tagged release
          # (current tag is v0.40.0; meson.build reports 0.40.99 as the dev
          # version ahead of v0.41.0). When v0.41.0 ships, set the
          # WIRELOG_VERSION repo variable to that tag and bump pyrewire to
          # match per the versioning rule.
          WIRELOG_VERSION: ${{ vars.WIRELOG_VERSION || 'main' }}
          WIRELOG_PREFIX: ${{ runner.temp }}/wirelog-install
        run: bash scripts/build_wirelog.sh

      - name: install pyrewire
        run: pip install -e ".[dev]"

      - name: pytest with coverage gate
        shell: bash
        env:
          WIRELOG_LIB: ${{ runner.temp }}/wirelog-install/lib/libwirelog.so.1
        run: |
          if [ "$RUNNER_OS" = "macOS" ]; then
            export WIRELOG_LIB=${{ runner.temp }}/wirelog-install/lib/libwirelog.1.dylib
          fi
          pytest -q
